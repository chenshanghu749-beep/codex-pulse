model_provider = "codeapi"
model = "gpt-5.6-sol"

[mcp_servers.example]
command = "example"

# >>> CodeAPI Status managed provider >>>
[model_providers.codeapi]
name = "CodeAPI"
base_url = "https://codeapi.nexita.net"
wire_api = "responses"

[model_providers.codeapi.auth]
command = "/bin/cat"
args = ["/Users/example/.codex/codeapi-status/codeapi.key"]
timeout_ms = 5000
# <<< CodeAPI Status managed provider <<<
